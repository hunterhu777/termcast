[package]
name = "termcast"
version = "0.1.0"
edition = "2021"
description = "A terminal media player for mp4 and webm"

[dependencies]
ffmpeg-next = "8.1"
cpal = "0.18"
crossterm = "0.29"
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[profile.release]
opt-level = 3
lto = true
